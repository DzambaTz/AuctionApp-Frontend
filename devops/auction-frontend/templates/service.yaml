apiVersion: v1
kind: Service
metadata:
  name: {{ include "auction.fullname" . }}
  labels:
    {{- include "auction.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "auction.selectorLabels" . | nindent 4 }}
