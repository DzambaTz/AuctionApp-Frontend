apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auction.fullname" . }}-test-connection"
  labels:
    {{- include "auction.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "auction.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
